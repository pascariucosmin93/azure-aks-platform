# Minimal Example

This example shows the smallest supported Azure AKS composition in this repository.

It includes:

- one resource group
- one VNet and AKS subnet
- one Log Analytics workspace
- one user-assigned managed identity
- one AKS cluster

Use it as a lightweight starting point before moving to `envs/dev` or `envs/prod`.
