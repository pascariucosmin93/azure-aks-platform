skip-check:
  - CKV_AZURE_164 # Trusted image enforcement depends on image signing and verification controls outside this infrastructure-only reference repository
  - CKV_AZURE_166 # ACR image scanning/mark-as-verified is not fully modeled in this reference repo
  - CKV_AZURE_117 # Disk Encryption Set requires additional CMK resources outside the scope of this simplified platform reference
  - CKV2_AZURE_32 # Key Vault private endpoint is intentionally omitted from this public reference to keep the network model compact
  - CKV_AZURE_217 # App Gateway keeps HTTP listener for HTTP->HTTPS redirect support in bootstrap scenarios
  - CKV_AZURE_218 # TLS policy/protocol hardening is deferred while module allows optional certificate wiring
  - CKV_AZURE_220 # Firewall IDPS deny mode requires Premium feature set not enforced in this reference baseline
  - CKV2_AZURE_57 # PostgreSQL Flexible Server uses delegated subnet + private DNS (private access model) instead of private endpoint
  - CKV2_AZURE_31 # Subnet NSG is attached via azurerm_subnet_network_security_group_association resource
  - CKV_AZURE_227 # Extra AKS node pools are created dynamically via for_each; host encryption is enforced in module defaults/inputs
  - CKV_AZURE_168 # max_pods is enforced via module defaults/inputs, but Checkov cannot resolve dynamic for_each expressions in this module
